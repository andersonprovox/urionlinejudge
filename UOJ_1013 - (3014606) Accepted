#include <stdio.h>
 
int main() {
 
   int a, b, c, x, maior;
	
	scanf("%d", &a);
	scanf("%d", &b);
	scanf("%d", &c);
	x = (a+b+abs(a-b))/2;
	maior = (x+c+abs(x-c))/2;
		
	printf("%d eh o maior\n", maior);
 
    return 0;
}
